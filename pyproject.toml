[project]
name = "git-try-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Swaraj Shinde", email = "swarajshinde2401@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
git-try-test = "git_try_test:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
